declare let process: {
  env: {
    NODE_ENV: string
    OPEN_AI_KEY: string
    CHATY_LANG: string
    ENGINE: string
    WEB_PORT: string
    NODE_PORT: string
    LAST_VERSION_CHECK_TIME: string
  }
}
